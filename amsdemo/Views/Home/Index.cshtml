@{
    ViewBag.Title = "Home Page";
   
}


<fieldset>
    @if (Session["UserName"] != null)
    {
        <text> Welcome @Session["UserId"].ToString() </text>
        <text> Welcome @Session["CompanyCode"].ToString() </text>
        <text> Welcome @Session["CityCode"].ToString() </text>

    }
</fieldset>